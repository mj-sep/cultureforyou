<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--seekbar 배경 -->
    <item android:id="@+id/background">
        <shape android:shape="line">
            <stroke android:width="5dp" android:color="@color/seekbar_background"/>
        </shape>
    </item>

    <!--seekbar progressbar -->
    <item android:id="@+id/progressbar">
        <clip>
            <shape android:shape="line">
                <stroke android:width="6dp" android:color="@color/seekbar_color"/>
            </shape>
        </clip>
    </item>
</layer-list>